{
  "name": "nymphea_bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "npx nodemon",
    "partial_build": "tsc",
    "build": "rimraf ./build && npm run partial_build",
    "complete_build": "npm run build && npm run register_cmds",
    "start": "npm run build && node build/index.js",
    "register_cmds": "npm run build && node ./build/register_cmds.js",
    "update": "git pull && npm i && npm run complete_build && npm start"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.3.0",
    "nodemon": "^2.0.22",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "console-stamp": "^3.1.1",
    "discord.js": "^14.11.0",
    "dotenv": "^16.1.4",
    "sqlite3": "^5.1.6"
  },
  "engines": {
    "node": "^12.20.0 || ^14.13.1 || >=16.0.0"
  },
  "type": "module"
}